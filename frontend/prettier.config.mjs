/** @type {import("prettier").Config} */
const config = {
  arrowParens: "always",
  singleQuote: false,
  trailingComma: "all",
  tabWidth: 2,
  semi: true,
  printWidth: 100,
};

export default config;
